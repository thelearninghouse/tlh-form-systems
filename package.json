{
  "name": "tlh-form-systems-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "vuepress dev .",
    "build": "vuepress build .",
    "deploy": "yarn build && git commit -m 'chore: new build' && git push"
  },
  "devDependencies": {
    "vuepress": "^1.0.2"
  }
}
